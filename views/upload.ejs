<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Upload CSV Files</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        h2,
        h3 {
            color: #333;
        }

        form {
            background-color: #fff;
            border-radius: 4px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        div {
            padding: 20px;
        }

        label {
            display: block;
            margin: 10px 0 5px;
            color: #333;
        }

        input[type="file"] {
            margin-bottom: 10px;
        }


        .delete-button {
            background: none;
            color: inherit;
            border: none;
            padding: 10px;
            font: inherit;
            cursor: pointer;
        }

        .update-button {
            padding: 10px 20px;
            background-color: #333;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        a {
            text-decoration: none;
            color: #333;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <h1>Trade Lens</h1>
    <h2>Upload CSV Files</h2>
    <form action="/upload" method="post" enctype="multipart/form-data">
        <div>
            <label>Companies CSV:</label>
            <input type="file" name="companies" accept=".csv" required><br>
            <label>Transactions CSV:</label>
            <input type="file" name="transactions" accept=".csv" required><br>
            <label>Historical CSV:</label>
            <input type="file" name="historical" accept=".csv" required><br>
            <button type="submit" class="update-button">Upload & Import</button>
        </div>

    </form>

    <p>Upload Duration: <%= uploadDuration %> ms</p>
    <p>SQL Import Duration: <%= sqlDuration %> ms</p>

    <% if (files.length) { %>
        <h2>Files in Bucket:</h2>
        <ul>
            <% files.forEach(name=> { %>
                <li>
                    <%= name %>
                        <form action="/delete" method="post" style="display:inline">
                            <div style="display:inline">
                                <input type="hidden" name="filename" value="<%= name %>">
                                <button type="submit" class="delete-button">Delete</button>
                            </div>
                        </form>
                </li>
                <% }) %>
        </ul>
        <% } %>

            <hr>
            <a href="/search">Go to Search</a>
</body>

</html>